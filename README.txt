The following directory contains the following files:

Pig Script Files(3):
1.AVGPIG- Calculates average cost per procedure
2.STATECOUNTPIG-Counts number of cases per procedure
3.CLUSTERPIG-Preprocessing of data before clustering 

Parsing File(1):
costpr.py-used to remove charater token '$'

K-Means(1):
readkmean.m is a matlab code to implement k-means algorithm

snap.dox contains the snapshots of the result

Paper(SA)- contains paper of the project

project_reportformat(SA) is the final report of the project.

Submitted by Sriram Anupindi(1MS12CS111) and TEAM.
